apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: emulation-manager
    component: orchestrator
  name: emulation-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      app: emulation-manager
  template:
    metadata:
      labels:
        app: emulation-manager
        component: orchestrator
    spec:
      containers:
      - command:
        - sleep
        - infinity
        env:
        - name: PYTHONUNBUFFERED
          value: '1'
        - name: SIMULATION_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        image: ghcr.io/ondrejman/emulator-manager:latest
        imagePullPolicy: Always
        name: manager
        resources:
          limits:
            cpu: 2000m
            memory: 2Gi
          requests:
            cpu: 500m
            memory: 512Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: false
          runAsGroup: 1000
          runAsNonRoot: true
          runAsUser: 1000
          seccompProfile:
            type: RuntimeDefault
        volumeMounts:
        - mountPath: /workspace
          name: workspace
        workingDir: /app
      serviceAccountName: emulation-manager
      volumes:
      - emptyDir: {}
        name: workspace
